   const data = this.data;
    const coord = data;
    var parameter_Start_index=data.indexOf(',');
    var action_URL = data.substring(0, parameter_Start_index);
    let long = action_URL;
    let lat =  data.split(",").pop();
    const markerCoord = {
      latitude: lat,
      longitude: long
    }